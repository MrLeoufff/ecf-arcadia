{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create new Animal</h1>
    <div class="form-control">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}
    </div>
    <a href="{{ path('app_animal_index') }}" class="btn btn-secondary mt-3">Back to list</a>
{% endblock %}

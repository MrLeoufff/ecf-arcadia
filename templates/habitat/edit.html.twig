{% extends 'base.html.twig' %}

{% block body %}
    <h1>Éditer Habitat</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Update') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_habitat_index') }}">Retour à la liste</a>

    {{ include('habitat/_delete_form.html.twig') }}
{% endblock %}
